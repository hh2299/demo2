<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.HrMapping">

    <select id="getHrList" resultType="com.example.demo.domain.dto.HrDTO">
        select
            h.*,
            c.name company
        from hr h
        left join company c on h.company_id = c.id
        where h.deleted = 0
        <if test="param.name!=null">
            and h.name like concat('%',#{param.name},'%')
        </if>
        <if test="param.age!=null">
            and h.age = #{param.age}
        </if>
        <if test="param.companyId!=null">
            and h.company_id = #{param.companyId}
        </if>
        <if test="param.company!=null">
            and c.name like concat('%',#{param.company},'%')
        </if>
        <if test="id!=null">
            and h.id = #{id}
        </if>

        order by h.update_time desc

    </select>
</mapper>
